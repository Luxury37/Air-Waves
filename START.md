# Air-Waves · 快速上手

## 1. 这是什么

一个**纯静态网页版「听觉节拍发生器」**：用 Web Audio API 实时合成**双耳节拍 / 等时节拍**，
为工作、学习、阅读时营造适合专注与放松的声音环境。

**风格**：清冷后朋克无线电 / CRT 终端。扫描线、噪点、暗角、闪烁与故障字叠加在冷色调场景上，
配合无线电刻度盘、VU 电平表和时域示波器，整体像一台老式短波接收机。
视觉素材为雪夜城市场景，可通过 `assets/` 下的备选图直接替换。

**主要功能**

| 功能 | 说明 |
| --- | --- |
| 频段预设 | THETA（Δf 7 Hz）· **ALPHA（Δf 10 Hz，默认）** · LOW BETA（Δf 15 Hz） |
| Δf 微调 | 4–18 Hz 连续可调 |
| 节拍模式 | **双耳**（左右声道差频）· **等时**（左右同频，按 Δf 通断脉冲） |
| 背景底噪 | 可选粉红噪声，用于掩蔽环境杂音，默认关闭 |
| 无爆音 | 振荡器全程常驻 + 平滑淡入淡出，播放/暂停/换频/切模式均无咔哒声 |
| 后台播放 | 切到其他标签页或最小化后继续发声 |
| 键盘 | `Space` 播放/暂停 · `M` 静音 · `Esc` 返回 · `1/2/3` 切频段 · `↑/↓` 音量 · `←/→` Δf |
| 响应式 | 桌面 / 平板 / 手机（含 320 px 窄屏） |

> ⚠️ **本工具仅用于放松与专注辅助，非医疗设备**，不用于诊断、治疗或预防任何疾病。
> 双耳节拍的主观感受因人而异，如出现头晕、耳鸣或任何不适，请立即停止使用。

---

## 2. 下载与运行

### 2.1 下载

在 GitHub 仓库页点 **Code → Download ZIP** 并解压，或：

```bash
git clone https://github.com/Luxury37/Air-Waves.git
```

### 2.2 运行

项目是纯静态页面，**无构建步骤、无依赖、无后端**。
但**必须通过 http 服务打开，不能直接双击 `index.html`** —— `file://` 协议下浏览器会拦截部分资源。

**Windows 一键启动（推荐）**：双击 **`Air-Waves.cmd`**
它会自动找一个空闲端口起服务并打开浏览器（需要已安装 Python 3）。

**其他方式**（在项目根目录执行，任选一种）：

```powershell
python serve.py 8765          # 推荐：自带的静态服务，禁用缓存
python -m http.server 8765    # 标准库，够用但会被浏览器启发式缓存
npx --yes serve -l 8765 .     # 需要 Node.js
```

也可以装 VS Code 的 **Live Server** 扩展，右键 `index.html` → Open with Live Server。

启动后浏览器打开 **http://127.0.0.1:8765/** 即可。

> 推荐 `serve.py`：它强制发送 `Cache-Control: no-store`，避免改完 `app.js` / `audio.js`
> 后刷新页面仍加载旧脚本。若之前用过 `python -m http.server`，按一次 `Ctrl+Shift+R` 清缓存即可。

### 2.3 第一次使用

1. 打开页面，点 **START / 开始** 进入播放界面 —— 此时**不会自动播放**，停在待机状态。
2. 再点 **PLAY** 才开始出声（浏览器要求音频必须由用户操作触发，这是预期行为，不是卡顿）。
3. **请佩戴耳机**。双耳节拍依赖左右声道差频，外放时左右声场混合，效果会明显减弱。
4. 音量从 **35%** 起步；长时间使用建议低于 50%。
5. 听到沙沙声时先看 README 第 5.4 节 —— 蓝牙耳机下这通常来自蓝牙编码，而非页面输出，
   打开 **AMBIENT NOISE** 一般能明显改善。

### 2.4 环境要求

- 支持 Web Audio API 的现代浏览器：Chrome / Edge 88+、Firefox 90+、Safari 14.1+
- Python 3（仅用于本地起服务，可用 Node.js 代替）

更多细节（音频实现、素材替换、背景图重新合成、已验证项）见 `README.md`。
